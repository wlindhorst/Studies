
@{
    ViewBag.Title = "Basic";
//    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Hello <span data-bind="text: name"></span></h2>

@section Scripts {
    <script>
        //function ViewModel() {
        //    this.name = 'Zippy Slug';
        //};

        //var viewModel = new ViewModel();
        // Or more simply:
        var viewModel = {
            name: 'Zippy Slug'
        };
        ko.applyBindings(viewModel);
    </script>
    }

